sudo: false
dist: trusty
language: ruby
rvm:
  - 2.2.8
  - 2.3.5
  - 2.4.2
  - ruby-head

addons:
  postgresql: "9.6"
  chrome: stable
  apt:
    sources:
      - sourceline: "ppa:mc3man/trusty-media"
      - sourceline: "ppa:ubuntuhandbook1/apps"
    packages:
      - ffmpeg
      - mupdf
      - mupdf-tools

script:
  - df -h
